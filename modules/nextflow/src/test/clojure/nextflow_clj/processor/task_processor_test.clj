(ns nextflow-clj.processor.task-processor-test)

(comment

  (ns nextflow-clj.processor.task-processor-test
    (:import (spock.mock MockingApi)
             (nextflow.script ProcessConfig)))

  (.Mock MockingApi ProcessConfig)

  '())
